# LangChain 中的六大核心组件

模型，位于 LangChain 框架的最底层，它是基于语言模型构建的应用的核心元素，因为所谓 LangChain 应用开发，就是以 LangChain 作为框架，通过 API 调用大模型来解决具体问题的过程。

## Model I/O

以把对模型的使用过程拆解成三块，分别是输入提示（对应图中的 Format）、调用模型（对应图中的 Predict）和输出解析（对应图中的 Parse）。这三块形成了一个整体，因此在 LangChain 中这个过程被统称为 Model I/O（Input/Output）。

1. 提示模板：使用模型的第一个环节是把提示信息输入到模型中，你可以创建 LangChain 模板，根据实际需求动态选择不同的输入，针对特定的任务和应用调整输入。
2. 语言模型：LangChain 允许你通过通用接口来调用语言模型。这意味着无论你要使用的是哪种语言模型，都可以通过同一种方式进行调用，这样就提高了灵活性和便利性。
3. 输出解析：LangChain 还提供了从模型输出中提取信息的功能。通过输出解析器，你可以精确地从模型的输出中获取需要的信息，而不需要处理冗余或不相关的数据，更重要的是还可以把大模型给回的非结构化文本，转换成程序可以处理的结构化数据。

### 提示模板

具体原则:

1. 给予模型清晰明确的指示
2. 让模型慢慢地思考

或者说

1. 写清晰的指示
2. 给模型提供参考（也就是示例）
3. 将复杂任务拆分成子任务
4. 给 GPT 时间思考
5. 使用外部工具
6. 反复迭代问题

使用 LangChain 和提示模板的好处是：

- 代码的可读性：使用模板的话，提示文本更易于阅读和理解，特别是对于复杂的提示或多变量的情况。
- 可复用性：模板可以在多个地方被复用，让你的代码更简洁，不需要在每个需要生成提示的地方重新构造提示字符串。
- 维护：如果你在后续需要修改提示，使用模板的话，只需要修改模板就可以了，而不需要在代码中查找所有使用到该提示的地方进行修改。
- 变量处理：如果你的提示中涉及到多个变量，模板可以自动处理变量的插入，不需要手动拼接字符串。
- 参数化：模板可以根据不同的参数生成不同的提示，这对于个性化生成文本非常有用。

### 输出解析

在开发具体应用的过程中，很明显我们不仅仅需要文字，更多情况下我们需要的是程序能够直接处理的、结构化的数据

**提示模板**可以嵌入对输出格式的定义，以便在后续处理过程中比较方便地处理已经被格式化了的输出

## 提示工程（上）

在这个提示框架中：

- 指令（Instuction）告诉模型这个任务大概要做什么、怎么做，比如如何使用提供的外部信息、如何处理查询以及如何构造输出。这通常是一个提示模板中比较固定的部分。一个常见用例是告诉模型“你是一个有用的 XX 助手”，这会让他更认真地对待自己的角色。
- 上下文（Context）则充当模型的额外知识来源。这些信息可以手动插入到提示中，通过矢量数据库检索得来，或通过其他方式（如调用 API、计算器等工具）拉入。一个常见的用例时是把从向量数据库查询到的知识作为上下文传递给模型。 提
- 示输入（Prompt Input）通常就是具体的问题或者需要大模型做的具体事情，这个部分和“指令”部分其实也可以合二为一。但是拆分出来成为一个独立的组件，就更加结构化，便于复用模板。这通常是作为变量，在调用模型之前传递给提示模板，以形成具体的提示。
- 输出指示器（Output Indicator）标记​​要生成的文本的开始。这就像我们小时候的数学考卷，先写一个“解”，就代表你要开始答题了。如果生成 Python 代码，可以使用 “import” 向模型表明它必须开始编写 Python 代码（因为大多数 Python 脚本以 import 开头）。这部分在我们和 ChatGPT 对话时往往是可有可无的，当然 LangChain 中的代理在构建提示模板时，经常性的会用一个“Thought：”（思考）作为引导词，指示模型开始输出自己的推理（Reasoning）。

## 提示工程（下）

CoT（Chain of Thought，思维链） 是一种提示工程技术，通过引导大语言模型（LLM）在给出最终答案前，先展示其逐步推理的过程，从而显著提升模型在复杂推理任务（如数学、逻辑、常识推理）上的表现。

**核心原理：为什么 CoT 有效？**
大模型本质上是“下一个词预测器”，直接回答复杂问题容易出错。CoT 通过以下方式提升性能：

1. 分解复杂问题：将多步推理任务拆解为一系列简单的中间步骤，降低每一步的难度。

2. 提高可解释性：模型的推理过程对用户透明，便于发现错误逻辑。

3. 激活模型的潜在推理能力：研究表明，LLM 在训练过程中已经学习了大量推理模式，CoT 提示能有效地“唤醒”这些能力。

### Few-Shot CoT

在提示词中提供几个完整的“问题 + 推理步骤 + 答案”示例，引导模型模仿这种格式来回答新问题。

```txt
示例 1：
问题：小明有 5 个苹果，小红给了他 3 个，小明现在有几个？
推理：5 + 3 = 8
答案：8 个

示例 2：
问题：一个班级有 20 个学生，其中 1/4 是女生，男生有多少人？
推理：女生人数 = 20 × 1/4 = 5 人，男生人数 = 20 - 5 = 15 人
答案：15 人

问题：一个商店有 100 个苹果，第一天卖出 30 个，第二天卖出剩余的一半，还剩多少个？
推理：
```

### 零样本 CoT (Zero-shot CoT)

不提供示例，直接通过在提示词中加入“让我们一步一步地思考”（Let's think step by step）来激发模型的推理能力。

```
问题：一个商店有 100 个苹果，第一天卖出 30 个，第二天卖出 20 个，还剩多少个？
让我们一步一步地思考：
```

## 进阶：CoT-SC（思维链自洽性）

CoT 的一个扩展技术是 CoT-SC（Self-Consistency，自洽性）。它通过采样多条推理路径，然后选择出现频率最高的答案作为最终输出，进一步提升推理的准确率。

原理：对于同一个问题，模型可能产生不同的推理路径。通过多次采样并投票，可以过滤掉偶然的错误路径，得到更可靠的答案。

### 核心原理

研究表明，大语言模型在推理时，单次生成的路径可能包含逻辑错误。然而，对于同一个问题，正确答案通常具有更高的“语义一致性”。即使中间推理步骤不同，最终正确的答案往往是同一个。

CoT-SC 的做法是：

1. 设置较高的温度（Temperature），比如 temperature=0.7，让模型的输出具有随机性。

2. 多次采样（如 5～10 次），生成多条带有推理过程的回答。

3. 解析出每条回答的最终答案。

4. 进行多数投票（Majority Vote），选出出现次数最多的答案作为最终输出。

## Tree of Thought

思维树（Tree of Thoughts，简称 ToT） 是思维链（CoT）的进阶版，由普林斯顿大学和谷歌 DeepMind 在 2023 年提出。如果说 CoT 是“一条路走到黑”的线性推理，那么 ToT 就是“走迷宫时，在岔路口不断探索、回溯、比较”的树状搜索推理。

它让模型不再满足于生成一条推理链，而是在**每一步生成多个候选分支，并通过搜索算法（如广度优先 BFS 或深度优先 DFS）全局探索，最终找到最优解。**

**核心机制**：四步循环

ToT 的标准流程包含四个反复执行的步骤：

1. 分解（Decomposition）：将复杂问题拆解为若干个思维步骤（Step），每一步都是一个相对独立的思考单元。

2. 生成（Generation）：在当前状态下，让模型生成 k 个不同的下一步候选思维（使用较高的 temperature）。

3. 评估（Evaluation）：让模型对每个候选思维进行打分（0~1），评估其通往最终正确答案的潜力。

4. 搜索（Search）：根据评估分数，使用 BFS（广度优先） 或 DFS（深度优先） 决定保留哪些分支、舍弃哪些分支，并继续向下探索。
